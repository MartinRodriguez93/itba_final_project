version: 2

sources:
    - name: staging
      database: gold-circlet-411323
      schema: source_dataset

      tables:
            - name: source_system_orders
              columns:
                - name: order_id
                  tests:
                    - unique
                    - not_null
                - name: customer_id
                  tests:
                    - relationships:
                        to: ref('customers')
                        field: id